<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!-- Hardened-runtime exceptions required to notarize a py2app bundle:
     CPython allocates writable+executable memory (ctypes/ffi) and the
     bundled .so extension modules would otherwise fail library validation.
     This app is NOT sandboxed - these are runtime exceptions only. -->
<dict>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
